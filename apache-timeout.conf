# QAProof — extend Apache Timeout so long-running PHP requests (regression
# polling, baseline capture) aren't killed mid-flight. Default Apache 2.4
# Timeout is 60s; we need ~15min to comfortably outlive a worst-case 30
# poll iterations × 25s each.
Timeout 900
KeepAliveTimeout 5
