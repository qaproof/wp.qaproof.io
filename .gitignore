# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Node (if you use build tools)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Composer (if used) — top-level only. Our qaproof/admin/js/vendor/ holds
# the pinned third-party JS libraries (Chart.js, jsPDF, jsPDF-AutoTable)
# we bundle to satisfy WP.org's "no external CDN" requirement, so it
# MUST ship with the plugin distribution.
/vendor/
composer.lock

# Build / dist
dist/
build/

# Env files
.env
.env.*

# IDE
.vscode/
.idea/

# Mac / Linux temp
*.swp
*.swo

# PHPUnit / coverage
coverage/
.phpunit.result.cache

# Archive files
*.zip
*.tar.gz